console.log('App is running!');

// JSX - JavaScript XML, JavaScript Syntax Extension
const template = <p>Indecision app</p>;
const appRoot = document.getElementById('app');

ReactDOM.render(template, appRoot);